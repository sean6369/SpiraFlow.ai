import { JournalEntry, DashboardStats, UserPattern } from '../types';
export declare function calculateDashboardStats(entries: JournalEntry[]): DashboardStats;
export declare function analyzeUserPatterns(entries: JournalEntry[]): UserPattern;
